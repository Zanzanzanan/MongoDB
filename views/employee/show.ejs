<!DOCTYPE html>
<html>
  <head>
    <title>Employee Detail</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://kit.fontawesome.com/a076d05399.js"></script> <!-- FontAwesome for Icons -->
  </head>
  <body>
    <div class="container">
      <h3><a href="/employees">⬅ Back to Employee List</a></h3>
      <div class="employee-detail">
        <h1>Employee Details</h1>
        <div class="detail-item"><i class="fas fa-user"></i> Name: <%= employee.name %></div>
        <div class="detail-item"><i class="fas fa-map-marker-alt"></i> Address: <%= employee.address %></div>
        <div class="detail-item"><i class="fas fa-briefcase"></i> Position: <%= employee.position %></div>
        <div class="detail-item"><i class="fas fa-dollar-sign"></i> Salary: $<%= employee.salary %>/year</div>
      </div>
      
      <h3><a href="/employees/edit/<%= employee._id%>">✏ EDIT</a></h3>
      <form action="/employees/delete/<%= employee._id%>" method="post">
        <button type="submit" class="delete-btn">🗑 DELETE</button>
      </form>
    </div>
  </body>
</html>
